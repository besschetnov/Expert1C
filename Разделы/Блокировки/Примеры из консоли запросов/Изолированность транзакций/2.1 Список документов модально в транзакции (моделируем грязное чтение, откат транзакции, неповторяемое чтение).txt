// 1. моделируем откат транзакции и грязное чтение:
// открыть список документов модально в транзакции, создать в нем 2 документа
// открыть в другом окне этот же список не модально и без транзакции, в нем открыть созданные документы
// закрыть модальный список, документы во втором списке исчезнут
//
// защита от грязного чтения: обе формы открываем модально в транзакции, в одной форме создаем доки, 
// во второй форме для 8.2 блокировка не даст эти доки прочитать, а для 8.3 созданные доки просто не будут видны при чтении
//		 
// 2. Моделируем неповторяемое чтение:		
// открыть список документов модально в транзакции
// в другом окне открыть список не модально, в нем помечать на удаление документы
// затем читаем эти изменения в списке, открытом в транзакции, все изменения будут видны
// в автоматическом режиме блокировок такого не будет
// в управляемом режиме можно защититься с помощью управляемой блокировки

НачатьТранзакцию();

	Ф = Документы.БезТабличныхЧастей.ПолучитьФорму("ФормаСписка");
	// в автоматическом режиме блокировок будут заблокированы все документы с Склад = Справочники.Склады.Розничный
	Ф.Отбор.Склад.Установить(Справочники.Склады.Розничный);

	// запрос в транзакции
	Ф.ОткрытьМодально();

ОтменитьТранзакцию();

//// управляемая блокировка
//Блок = Новый БлокировкаДанных;
//ЭлемБлокировки = Блок.Добавить("Документ.БезТабличныхЧастей");
//ЭлемБлокировки.УстановитьЗначение("Склад", Справочники.Склады.Розничный);
//ЭлемБлокировки.Режим = РежимБлокировкиДанных.Исключительный;
//Блок.Заблокировать();
