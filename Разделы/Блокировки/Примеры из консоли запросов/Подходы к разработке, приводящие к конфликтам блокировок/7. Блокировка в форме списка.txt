// в одном окне открываем НЕ модально (в обычном режиме) форму списка "СТабличнымиЧастями"
// в обработчике ПриВыводеСтроки() или ПриПолученииДанных() прописано получение через точку реквизита

// в другом окне записываем любой из документов "СТабличнымиЧастями" в транзакции (код ниже), 
// либо записываем в форме списка, открытой модально в транзакции (предыдущий пример)

// после этого ждем 20 сек и обновляем список документов в первом окне
// возникнет блокировка во всех режимах, кроме RCSI
// если не возникает, то промотать список, чтобы найти прочитанный документ

// 1. В первом окне 
Запрос = Новый Запрос("ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка ИЗ Документ.СТабличнымиЧастями УПОРЯДОЧИТЬ ПО Дата УБЫВ");
Выб = Запрос.Выполнить().Выбрать();

Пока Выб.Следующий() Цикл
	
	Док = Выб.Ссылка.ПолучитьОбъект();
	
	НачатьТранзакцию();
	
	Док.Записать();	// X - Lock
	
	Предупреждение("Записан документ "+Док);
	
	ОтменитьТранзакцию();
	
КонецЦикла;

// 2. Во втором окне
Ф = Документы.СТабличнымиЧастями.ПолучитьФормуСписка();
Ф.Открыть();

