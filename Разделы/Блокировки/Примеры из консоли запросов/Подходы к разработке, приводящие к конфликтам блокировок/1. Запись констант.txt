// константа - это таблица с одной записью, ее нельзя менять одновременно в двух транзакциях
// до 8.2.14 нельзя было поменять в двух транзакциях даже разные константы, 
// потому что до 8.2.14 все константы хранились в одной таблице
// после 8.2.14 каждая константа хранится в отдельной таблице
// 
// также еще раз упомянуть, что модальные окна в транзакции - грубая ошибка 
// и могут использоваться лишь для отладки и демонстрации

НачатьТранзакцию();

	Константы.КонстантаЧисло.Установить(123);

	Предупреждение("Ждем...");
	
ЗафиксироватьТранзакцию();
