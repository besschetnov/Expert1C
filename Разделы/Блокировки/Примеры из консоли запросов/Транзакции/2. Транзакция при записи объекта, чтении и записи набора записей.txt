// выполнить код, снять трассировку и убедиться, 
// что есть транзакция при записи объекта и чтении набора записей

Запрос = Новый Запрос("ВЫБРАТЬ ПЕРВЫЕ 1 Док.Ссылка ИЗ Документ.БезТабличныхЧастей КАК Док");

Выб = Запрос.Выполнить().Выбрать();
Выб.Следующий();

Набор = РегистрыНакопления.ПустойРегистр.СоздатьНаборЗаписей();
Набор.Отбор.Регистратор.Установить(Выб.Ссылка);

// транзакция при чтении набора записей
Набор.Прочитать();
Предупреждение("Прочитан набор записей");

// транзакция при записи набора записей
Набор.Записать();
Предупреждение("Записан набор записей");

Объект = Выб.Ссылка.ПолучитьОбъект();

// транзакция при записи объекта
Объект.Записать(РежимЗаписиДокумента.Запись);
Предупреждение("Записан объект");
