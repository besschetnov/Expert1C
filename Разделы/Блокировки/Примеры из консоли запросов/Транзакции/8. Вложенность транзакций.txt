// проверить в трассировке, сколько транзакций будет открыто и когда будет откат транзакции

НачатьТранзакцию(); // 1
	НачатьТранзакцию(); // 2
	
		НачатьТранзакцию(); // 3
	    Предупреждение("Начал три транзакции");
		а = Константы.КонстантаЧисло.Получить();
			
		ОтменитьТранзакцию(); // 3
		
	    Предупреждение("Транзакция 3  ''отменена''");
			
	Попытка 
		а = Константы.КонстантаЧисло.Получить(); 
	Исключение 
		Предупреждение("Попытка обращения к константе: "+ОписаниеОшибки()); 
	КонецПопытки;
		
	Предупреждение("Транзакция 2 активна: " + ТранзакцияАктивна());
	
	ЗафиксироватьТранзакцию();  // 2
	
Предупреждение("Транзакция 1 активна: " + ТранзакцияАктивна());

ЗафиксироватьТранзакцию(); // 1, только в этот момент произойдет Rollback Transaction


