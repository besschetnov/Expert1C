НачатьТранзакцию();

	Блок = Новый БлокировкаДанных;

	ЭлемБлокировки = Блок.Добавить("Справочник.Организации");
	// такую блокировку (по коду) можно установить, если сделать доп настройку в свойствах объекта в конфигураторе
	// по умолчанию можно установить блокировку только по полю Ссылка
	ЭлемБлокировки.УстановитьЗначение("Код", "100500");

	ЭлемБлокировки.Режим = РежимБлокировкиДанных.Исключительный;
	
	Блок.Заблокировать();
	
	Предупреждение("Заблокировали запись справочника по коду");
	
ЗафиксироватьТранзакцию();
