// выполнить в двух окнах в УПРАВЛЯЕМОМ режиме блокировок
// в первом окне выбрать запросом две ссылки в прямом порядке
// во втором окне выбрать запросом две ссылки в обратном порядке

Запрос = Новый Запрос("ВЫБРАТЬ ПЕРВЫЕ 2 Ссылка, Дата ИЗ Документ.БезТабличныхЧастей");
ТаблицаЗапроса = Запрос.Выполнить().Выгрузить();
ТаблицаЗапроса.Сортировать("Дата ВОЗР"); // во втором окне заменить ВОЗР на УБЫВ
ТаблицаЗапроса.ВыбратьСтроку();

Объект1 = ТаблицаЗапроса[0].Ссылка.ПолучитьОбъект();
Объект2 = ТаблицаЗапроса[1].Ссылка.ПолучитьОбъект();

НачатьТранзакцию();

	// шаг 1
	Объект1.Записать();
	Предупреждение("Шаг 1 Записан объект");

	// шаг 2
	Объект2.Записать();
	Предупреждение("Шаг 2 Записан объект");
	
ОтменитьТранзакцию();
